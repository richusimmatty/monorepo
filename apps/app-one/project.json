{
  "name": "app-one",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/app-one/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project app-one --web",
  "targets": {
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f apps/app-one/Dockerfile -t ${DOCKER_USERNAME:-richusimmatty}/app-one:latest -t ${DOCKER_USERNAME:-richusimmatty}/app-one:${GIT_SHA:-local} . && ([ -n \"$DOCKER_REGISTRY\" ] && docker push ${DOCKER_USERNAME:-richusimmatty}/app-one:latest && docker push ${DOCKER_USERNAME:-richusimmatty}/app-one:${GIT_SHA:-local} || true)"
      }
    }
  }
}