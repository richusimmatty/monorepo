{
  "name": "app-two",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/app-two/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project app-two --web",
  "targets": {
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f apps/app-two/Dockerfile -t ghcr.io/${DOCKER_USERNAME:-richusimmatty}/app-two:latest -t ghcr.io/${DOCKER_USERNAME:-richusimmatty}/app-two:${GIT_SHA:-local} . && ([ -n \"$DOCKER_REGISTRY\" ] && docker push ghcr.io/${DOCKER_USERNAME:-richusimmatty}/app-two:latest && docker push ghcr.io/${DOCKER_USERNAME:-richusimmatty}/app-two:${GIT_SHA:-local} || true)"
      }
    }
  }
}